document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".main-nav");if(toggle&&nav){toggle.addEventListener("click",()=>{const open=nav.classList.toggle("is-open");toggle.setAttribute("aria-expanded",open);});}document.querySelectorAll("[data-year]").forEach(el=>el.textContent=new Date().getFullYear());const buttons=document.querySelectorAll(".audience-btn");buttons.forEach(btn=>btn.addEventListener("click",()=>{buttons.forEach(b=>b.classList.remove("active"));btn.classList.add("active");document.body.dataset.audience=btn.dataset.audience||"all";}));});function calculateBMI(){const w=parseFloat(document.getElementById("bmi-weight")?.value),h=parseFloat(document.getElementById("bmi-height")?.value);const out=document.getElementById("bmi-result");if(!w||!h||h<=0){out.textContent="Enter a valid weight and height.";return;}const bmi=w/((h/100)**2);let band=bmi<18.5?"below the usual adult reference range":bmi<25?"within the usual adult reference range":bmi<30?"above the usual adult reference range":"in the obesity range";out.textContent=`BMI: ${bmi.toFixed(1)} — ${band}. BMI is a screening measure, not a diagnosis.`;}